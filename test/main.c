#include <sys/param.h>
#include <sys/systm.h>
#include <sys/proc.h>

#include <sys/mbuf.h>
#include <sys/socket.h>
#include <sys/sockio.h>

#include <net/if.h>
#include <net/netisr.h>
#include <net/route.h>

#ifdef INET
#include <netinet/in.h>
#include <netinet/in_systm.h>
#include <netinet/in_var.h>
#include <netinet/ip.h>
#include <netinet/if_ether.h>
#endif
#include <net/if_wp.h>
#include <test/vars.h>


extern void ut_event();

//ut_timeout
#if 0
void tm_tester()
{
	printf("\n Aha");
}

void ut_timeout()
{
	init_timer();
	timeout(tm_tester, "aha", 1000);
	Sleep(3000);
}
#endif

// a udp pkt with ethernet header 
//eth_header: 0x00, 0x50, 0x56, 0x87, 0x60, 0x02, 0x94, 0xde, 0x80, 0x70, 0x65, 0xdf, 0x08, 0x00,
//used for testing purpose, length = 0xb4(180)
char audp_pkt[] =
{
	
};

#define false 0
#define true 1
struct mbuf*
	mbuf_copyin(char *buf, unsigned blen)
{
	struct mbuf *m = NULL, *fm = NULL;
	struct mbuf **mp = NULL;
	int resides = blen;
	int space, len;
	int hdr = true;
	unsigned char *p = NULL;

	do {
		if (hdr) {
			MGETHDR(m, 0, 0);
			fm = m;
			mp = &m->m_next;
			hdr = false;
			space = MHLEN;
		}
		else {
			MGET(m, 0, 0);
			*mp = m;
			mp = &m->m_next;
			space = MLEN;
		}

		len = min(resides, space);
		p = mtod(m, unsigned char*);
		bcopy(&buf[blen - resides], p, len);
		m->m_len = len;
		space -= len;
		resides -= len;
	} while (resides > 0);

	fm->m_pkthdr.len = blen;
	fm->m_pkthdr.rcvif = NULL;
	return fm;
}

void ut_sendapkt()
{
	struct mbuf *m;

	m = mbuf_copyin(audp_pkt, sizeof(audp_pkt));
	wpif->if_ioctl(wpif, SIOCSIFADDR, &tifaddr);
	tifaddr.ia_addr.sin_addr.s_addr = 0x9901a8c0;
	wpif->if_output(wpif, m, &hcdst, 0);

}

//ut_timeout
#if 0
void tm_tester()
{
	printf("\n Aha");
}

void ut_timeout()
{
	init_timer();
	timeout(tm_tester, "aha", 1000);
	Sleep(3000);
}
#endif

void
ut_ioctl_ifaddr()
{
    tifaddr.ia_addr.sin_addr.s_addr = 0x9901a8c0;
	wpif->if_ioctl(wpif, SIOCSIFADDR, &tifaddr);
}

void
ut_ioctl_up()
{
    struct ifreq ifr = {0};
	struct proc p = {0};

    printf("\nut_ioctl_up");
	memcpy(ifr.ifr_name, "winpcap_intf1", sizeof("winpcap_intf1"));
    ifr.ifr_flags |= IFF_UP;
    ifioctl(NULL, SIOCSIFFLAGS, &ifr, &p);
}

void
ut_ioctl_down()
{
    struct ifreq ifr = {0};
	struct proc p = {0};
    
    printf("\nut_ioctl_down");    
	memcpy(ifr.ifr_name, "winpcap_intf1", sizeof("winpcap_intf1"));
    ifr.ifr_flags &= ~IFF_UP;
    ifioctl(NULL, SIOCSIFFLAGS, &ifr, &p);
	
}

void main()
{
    main1();
    ut_ioctl_up();
    start_recv();

    return;
}


